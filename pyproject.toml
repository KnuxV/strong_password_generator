[project]
name = "testing-username-password"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pdoc>=16.0.0",
    "pytest>=9.0.2",
]
